file(GLOB COMMON_SRCS "*.c")

if (NOT ${CMAKE_BUILD_TYPE} STREQUAL Debug)
  list(REMOVE_ITEM COMMON_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/debug.c")
endif()
list(REMOVE_ITEM COMMON_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/object.c")
list(REMOVE_ITEM COMMON_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/psfonts.c")

set(COMMON_SRCS ${COMMON_SRCS} PARENT_SCOPE)
